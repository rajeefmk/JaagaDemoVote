<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/LinearLayout1"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/bit"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:gravity="center"
    tools:context=".ProjectList" >

    <ListView
    android:id="@android:id/list"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" 
    android:divider="@android:color/transparent"
	android:dividerHeight="10dp"
	android:fadingEdge="none"
    android:listSelector="@android:color/transparent"
    />
    <TextView
       android:id="@android:id/empty"
       android:layout_width="match_parent"
       android:layout_height="match_parent"
       android:text="You are not connected to World Wide Web.May be World Wide Web doesn't like you. Oh wait !! May be your router ran out of Data ? Use this moment to look back into past.May be this is karma coming back to bite you, for wasting all those bandwidth. Anyway, Wait till next month for data recharge. 
       Or.... May be just turn-on Wifi in your phone !!" 
       android:gravity="center"
       android:textSize="19sp"
       android:textColor="#fff"
       android:background="@drawable/bit"
       />

</LinearLayout>